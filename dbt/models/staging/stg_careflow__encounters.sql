select
    encounter_key,
    encounter_id,
    patient_key,
    patient_key_is_missing,
    provider_key,
    provider_key_is_missing,
    organization_key,
    organization_key_is_missing,
    payer_key,
    payer_key_is_missing,
    encounter_date_key,
    start_timestamp,
    stop_timestamp,
    encounter_class,
    encounter_duration_minutes,
    base_encounter_cost,
    total_claim_cost,
    payer_coverage,
    patient_responsibility,
    patient_responsibility_is_negative,
    is_inpatient,
    is_emergency,
    reason_code,
    reason_description
from {{ source('careflow_fact', 'fact_encounter') }}
