# dashboard/assets/

Static assets for the CareFlow Analytics dashboard (custom logo, favicon,
or CSS, if added later). Empty for now -- the dashboard currently uses
Streamlit's built-in theming (`dashboard/.streamlit/config.toml`) and a
text-based header, deliberately avoiding fabricated logos or stock
imagery. Add real, licensed assets here if the project acquires them.
