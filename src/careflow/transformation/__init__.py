"""Bronze-to-Silver transformation utilities for CareFlow Analytics.

This package cleans and standardizes Bronze Parquet datasets into
analytics-ready Silver Parquet datasets, driven by a declarative schema
registry rather than ad-hoc per-file logic.
"""
